# Project instructions

本文件是本项目及其后续维护工作的执行规范。它也可以复制到同一工作区中的其他求职项目根目录，确保项目一、项目二、项目三使用一致的交付标准。

## Git 提交

- 每次完成一个可识别的改动后，必须创建一个对应的 Git commit。
- commit 信息应简洁、准确地说明改动内容，便于以后追踪、回滚和定位问题。
- 提交前检查 `git diff` 和 `git status`，不要把无关文件混入提交。
- 禁止提交密钥、密码、`.env`、用户上传的机密资料、Embedding 缓存和本地反馈数据。

## 测试与验证

- 每次改动完成后，必须编写或更新与改动相关的测试。
- 交付给用户前，必须运行所有相关测试、评测和必要的手工验证。
- 只有在测试和验证通过后，才能将改动交付或发布。
- 如果测试无法运行，必须说明原因、影响范围和下一步处理方式，不得把未验证的结果描述为已完成。

## 项目交付标准

- 优先交付可运行、可演示、可复现的最小版本，再逐步迭代。
- 产品行为应有文档说明，重要结论应保留来源或验证证据。
- 不伪造用户反馈、测试结果、性能数据或商业结果。
- 外部 API 的密钥只允许通过环境变量或部署平台的 Secrets 配置，不得写入代码、文档或 Git 历史。
